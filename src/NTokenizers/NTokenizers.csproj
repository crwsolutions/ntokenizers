<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks>netstandard2.0</TargetFrameworks>
		<LangVersion>latest</LangVersion>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<IsPackable>true</IsPackable>
		<SuppressDependenciesWhenPacking>true</SuppressDependenciesWhenPacking>
		<!-- NuGet package metadata -->
		<PackageId>NTokenizers</PackageId>
		<VersionSuffix Condition="'$(VersionSuffix)' == ''">preview</VersionSuffix>
		<Version>0.3-$(VersionSuffix)</Version>
		<Authors>crwsolutions</Authors>
		<Copyright>(c) crwsolutions</Copyright>
		<PackageLicenseExpression>MIT</PackageLicenseExpression>
		<Description>Collection of stream-capable tokenizers for XML, JSON, Markup, TypeScript, C# and SQL processing. See README for usage and details.</Description>
		<PackageDescription>Collection of stream-capable tokenizers for XML, JSON, Markup, TypeScript, C# and SQL processing. See README for usage and details.</PackageDescription>
		<PackageTags>tokenizer;xml;json;md;markup;typescript;c#;csharp;sql; stream;parsing;dotnet</PackageTags>
		<RepositoryUrl>https://github.com/crwsolutions/ntokenizers</RepositoryUrl>
		<RepositoryType>git</RepositoryType>
		<PackageReadmeFile>README.md</PackageReadmeFile>
		<PackageProjectUrl>https://crwsolutions.github.io/ntokenizers</PackageProjectUrl>
		<Title>NTokenizers</Title>
		<PackageReleaseNotes>finalized api</PackageReleaseNotes>
		<GenerateDocumentationFile>true</GenerateDocumentationFile>
	</PropertyGroup>

	<ItemGroup>
		<None Include="..\..\README.md">
			<Pack>True</Pack>
			<PackagePath>\</PackagePath>
		</None>
	</ItemGroup>

</Project>
